<!DOCTYPE html>
<html lang="en" class="[scrollbar-gutter:stable]">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title suffix={assigns[:page_suffix] || " · Affinew"}>{ assigns[:page_title] || "Explore High Ticket affiliate programs" }</.live_title>
    <%= if assigns[:meta_tags] do %> 
      <.dynamic_tag :if={is_map(tag_attrs)} :for={tag_attrs <- assigns[:meta_tags]} name="meta" {tag_attrs} /> 
    <% end %>
    <link rel="icon" href={assigns[:favicon] || "/favicon.ico"} type="image/x-icon">
    <link rel="preconnect" href="https://rsms.me/">
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
    <link :if={!assigns[:noappcss]} phx-track-static rel="stylesheet" href={"/assets/app.css"} />
    <script :if={!assigns[:noappscript]} defer phx-track-static type="text/javascript" src={"/assets/app.js"}></script>
  </head>
  <body class="bg-white">
    { @inner_content }
  </body> 
</html>

<!DOCTYPE html>
<html lang="en" class="[scrollbar-gutter:stable] h-full bg-white">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title suffix={assigns[:page_suffix] || " · Phoenix Framework"}>
      { assigns[:page_title] || "Monorepo" }
    </.live_title>
    <%= if assigns[:meta_tags] do %> 
      <.dynamic_tag :for={tag_attrs <- assigns[:meta_tags]} :if={is_map(tag_attrs)} name="meta" {tag_attrs} /> 
    <% end %>
    <link rel="icon" href={assigns[:favicon] || "favicon.ico"} type="image/x-icon">
    <link rel="preconnect" href="https://rsms.me/">
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
    <link phx-track-static rel="stylesheet" href={"/assets/#{assigns[:page_css] || "app"}.css"} />
    <script defer phx-track-static type="text/javascript" src={"/assets/#{assigns[:page_js] || "app"}.js"}></script> 
  </head>
  <body class="bg-white h-full">
    { @inner_content }
  </body> 
</html>
